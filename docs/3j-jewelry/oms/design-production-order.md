# Design — ใบผลิตเข้าสต็อก (P1) · 17 ก.ย. 69

> ที่มา: เจ้าของสั่ง 17 ก.ย. — *"เรื่องระบบแยกของผลิตมาขายปลีกเอง กับสินค้าทำใบเสนอราคาเหมือนเดิม"*
> design โดย architect · ตรวจและตัดสินโดย Tech Lead · **ยังไม่ implement** — ติด Q1 ที่เจ้าของต้องตอบ
> โครงเดิมอยู่ที่ [system-flow-2026-09.md](system-flow-2026-09.md) §6.1 / §7 / §8 (แหล่งจริงของมติ)

## สิ่งที่ตรวจจากของจริงก่อนออกแบบ (ไม่ใช่สมมติฐาน)

| พบ | ผลต่อ design |
|---|---|
| แถว `central_stock` ถูกสร้างเฉพาะตอน `/products/new` — SKU generator / `product_upsert` / import auto-create **ไม่สร้าง** | `adjust_stock(+qty)` บน SKU ใหม่จะ raise *"would go negative"* (ข้อความหลอก) ⇒ RPC done ต้อง ensure แถวก่อนเสมอ |
| OEM ใช้ `analytics.oem_*` และ **ไม่อ่านต้นทุนจาก `public.product`** | สองสายแยกกันอยู่แล้ว — P1 แตะไฟล์ OEM **0 ไฟล์** |
| ราคาเงินวันนี้อ่านได้จาก `oem_metal_price` (manual ชนะ auto จัดการใน 0127/0128 แล้ว) | ไม่ต้องเขียน logic ซ้ำ · แต่ **ห้ามให้ใบผลิตไปกรอก manual ที่ `/oem/rates`** เพราะจะล็อกราคาใบเสนอราคา OEM ทั้งวันเป็นผลข้างเคียง ⇒ ใบผลิตมีช่อง override ของตัวเอง |
| SKU โหมด `spot` = ต้นทุนลอยตามราคาเงินรายวัน | ถ้า done แล้วปล่อยเป็น spot จะขัดกฎเหล็ก "stamp" ⇒ ดู Q1 |

## ✅ Q1 — เจ้าของตอบแล้ว 17 ก.ย. 69: **"ล๊อค ตามนี้เลย"**

คำถาม: *"หลังผลิตเสร็จ ต้นทุน SKU ใน `/catalog` ล็อกตามใบผลิต (ไม่ลอยตามราคาเงินรายวัน) ใช่ไหม"*

**มติ: ล็อก** ⇒ RPC `done` เซ็ต `cost_type='fixed'` + `unit_cost` = ต้นทุนที่คำนวณ ณ วันที่ผลิตเสร็จ
ราคาเงินขึ้นวันหลัง **ไม่กระทบกำไรของล็อตที่ผลิตไปแล้ว** — ตรงกับกฎเหล็กเดิมของเจ้าของ
*"stamp ราคาทุนตามนี้ไปเลย อนาคตถ้ามาปรับราคาไม่ต้องไปแก้กำไรย้อนหลัง"*

ทางที่ถูกปฏิเสธ (ห้ามเสนอซ้ำ): คง `cost_type='spot'` ตามที่ §6.1 เขียนไว้เดิม —
ต้นทุนจะลอยตามราคาเงินทุกวัน แล้วกำไรย้อนหลังขยับทุกครั้งที่ re-import เดือนเก่า

## มติที่ Tech Lead ตัดสินแล้ว (ไม่ต้องถามเจ้าของ)

**1. ลำดับงานเปลี่ยนเป็น P1 → P1.5 → P2** (เดิม P1 → P2 → P1.5)
เหตุผล: ทุกวันที่ P1 อยู่โดยไม่มี P1.5 คือวันที่สต็อก **บวกอย่างเดียวไม่มีวันลด** เจ้าของจะเห็นตัวเลขที่ดูเหมือนจริงแต่ผิด ซึ่งแย่กว่าไม่มีตัวเลข · และ P1.5 หดลงแล้วเพราะคอลัมน์ `track_stock` ย้ายมาอยู่ใน P1

**2. `track_stock` — ย้าย *คอลัมน์* มา P1 แต่ไม่ย้าย RPC toggle**
P1 เพิ่ม `product.track_stock` + `track_stock_since` และให้ RPC done เป็นผู้เปิด · ส่วน toggle บน `/catalog` + ยอดตั้งต้น + guard แม่/ลูก ยังเป็นงานของ P1.5
เหตุผลที่ไม่ปล่อยให้ P1.5 ทำทั้งก้อน: ถ้า P1 ไม่จดวันเริ่มนับ ยอดขายช่วง PO→P1.5 จะถูกตัดซ้ำหรือไม่ถูกตัดเลย

**3. "บวกอย่างเดียว" ชั่วคราว — ยอมรับได้ ภายใต้ 3 เงื่อนไขที่ design บังคับ**
(ก) ตัวเลข **self-heal** — `track_stock_since` + `fact_order_item` ครบ ⇒ reconcile รอบแรกของ P1.5 หักย้อนหลังให้เองโดยไม่ต้องนับของใหม่
(ข) **UI ของ P1 ห้ามมีคำว่า "คงเหลือ" ที่ไหนเลย** — แสดงเฉพาะข้อเท็จจริงของใบ ("ผลิตเข้าแล้ว N ชิ้น เมื่อ …") + ป้ายบน `/stock` ว่ายังไม่หักยอดขาย
(ค) ตอน UAT บอกเจ้าของตรงๆ ว่ารอบนี้ให้ **ต้นทุน + ประวัติผลิต + จุดเริ่มนับ** ยังไม่ให้ยอดคงเหลือ

**4. ข้อครหาว่าขัดกฎ "ห้ามเขียนกลับ catalog" — ไม่ขัด**
กฎนั้น ([[sku-autofill-decisions]] 26 ส.ค.) พูดถึง **ใบเสนอราคา OEM** ซึ่งเป็น snapshot ของดีล · ใบผลิตคือ *เหตุการณ์ที่ทำให้เกิดต้นทุน* และเจ้าของสั่งเองเมื่อ 17 ก.ย. ว่า done ⇒ cost + stock
**บันทึกไว้ที่นี่เพื่อไม่ให้มีใครมาเถียงซ้ำอีกรอบ**

## โครงสร้างที่ตกลงแล้ว

**ตาราง** `analytics.production_order` + `analytics.production_order_item`
- เลขใบ `PO-0001` เป็น generated column จาก `seq` (unique ต่อร้าน) — **ไม่ใช้ `oem_doc_counter`** เพราะไม่ใช่เอกสารทางกฎหมาย เลขข้ามได้
- สถานะ `open → done` / `open → cancelled` · **done และ cancelled เป็นปลายทาง แก้/ลบไม่ได้** บังคับด้วย trigger ไม่ใช่แค่ UI
- `unique (production_order_id, product_id)` — กัน SKU เดิมสองบรรทัดในใบเดียวที่ระดับ schema
- เก็บ `prev_cost_type` / `prev_unit_cost` / `prev_*` ไว้บนใบ ⇒ ตรวจย้อนได้ว่า stamp ทับอะไร โดยไม่ต้องพึ่ง audit log

**RPC 8 ตัว** (service_role เท่านั้น · `search_path` ตรึง · revoke anon/authenticated)
`save` · `item_set` · `item_remove` · `cost_calc` · `spot_resolve` · `preview` · **`done` 💰** · `cancel`
- `preview` ใช้โค้ดเส้นเดียวกับ `done` ลบส่วนเขียน ⇒ **ที่เห็นก่อนกด = ที่จะถูก stamp**
- `done` ทำทั้งหมดใน **1 transaction**: validate ทุกบรรทัดก่อนแตะสต็อกบรรทัดแรก → ensure `central_stock` → `adjust_stock` → เขียนต้นทุนกลับ → พลิกสถานะเป็นบรรทัดสุดท้าย · ล้มตรงไหน rollback หมด ⇒ "ต้นทุน + สต็อก ในจังหวะเดียว" เป็นจริงตามตัวอักษร
- `spot_resolve` — override > ราคาวันนี้ > **raise** · **ห้าม fallback ราคาเมื่อวาน**

**idempotency** `po:<item_id>` เพียงพอ เพราะมี state machine + unique constraint หนุน
กด done ซ้ำ → รอ row lock → เห็น done → คืนผลเดิม ไม่ raise · cancel แล้ว done ใหม่ **เป็นไปไม่ได้** (cancelled เป็นปลายทาง)

**หน้าจอ** section ใหม่ `/production` แยก layout + เมนูจาก OEM ทั้งหมด
`/production` (รายการ) · `/production/new` · `/production/[id]` (แก้ได้ขณะ open · dialog กรอกจำนวนที่ผลิตได้จริง + โชว์ต้นทุนใหม่เทียบเดิมก่อนกดยืนยัน)

## หนี้ที่รู้ตัวตั้งแต่ design

1. **P1.6** — re-import เดือนเก่าหลังมีใบผลิตใบที่ 2 ของ SKU เดิม จะ re-cost ออเดอร์เก่าของ SKU นั้น (transform ลบแล้ว insert ใหม่) · วันนี้ผลกระทบ ≈ 0 เพราะ "เอาเฉพาะของใหม่" (ขายก่อนผลิตไม่ได้) และ **ก่อนมี P1 ช่องนี้เปิดกว้างกว่า** เพราะ spot ลอยทุกวัน ⇒ P1 ทำให้แคบลง ไม่ใช่กว้างขึ้น
2. `stock_ledger.move_type` ยังเป็น `adjustment` — แยกที่มาด้วย prefix `po:` ไปก่อน (ไม่เพิ่ม enum เพราะต้อง replace `adjust_stock` ที่ผ่าน security แล้ว)
3. ไม่มี "ยกเลิกหลัง done" — สต็อกผิดแก้ที่ `/stock` · ต้นทุนผิดแก้ที่ `/catalog` · ต้องผลิตเพิ่มเปิดใบใหม่
4. `/catalog` ยังไม่มีป้ายว่า "ต้นทุนมาจากใบ PO-xxxx"

## เคสที่ต้องไม่ผ่าน (ส่ง security/QA ต่อได้เลย)

20 เคส ครอบคลุม: จำนวนติดลบ/NaN/เกินเพดาน · SKU คนละร้าน · **SKU `^live` (เช็คทั้งตอนใส่ในใบและตอน done เผื่อถูก rename ทีหลัง)** · SKU ปิดใช้งาน · SKU ซ้ำในใบ · done ซ้ำ/done+cancel ชนกัน · แก้ใบที่ปิดแล้วผ่าน service key ตรงๆ · ใบว่าง/ทุกบรรทัดเป็น 0 · spot แต่ไม่มีน้ำหนัก · **ราคาเงินวันนี้ไม่มีและไม่มี override** · override นอกช่วง 5–500 · staff เรียก action ตรง · anon/authenticated เรียก RPC ผ่าน REST

**เคสที่ต้อง "ไม่พัง" ด้วย**: done บน SKU ที่ยังไม่มีแถว `central_stock` (คือ SKU ทุกตัวจาก generator) ต้องผ่าน · done บน SKU ที่เปิดนับสต็อกอยู่แล้ว `track_stock_since` ต้องไม่ถูกเลื่อน · หน้า OEM ทั้งหมดต้องทำงานเหมือนเดิม

## ลำดับ implement

**P1a** backend — migration เดียว (schema + trigger + 8 RPC + 2 view + grants) → **dry-run ใน transaction rollback** → security → apply + merge
**P1b** frontend — 3 หน้า + layout + nav + actions → QA ระดับ L (แตะ `product` + ledger) → UAT

---

## รอบ 0132 — ปิด 3 finding ของ security (apply แล้ว 18 ก.ย. 69)

| finding | เดิม | แก้เป็น |
|---|---|---|
| **M1** ราคาเงินเลื่อนระหว่าง preview → done | done resolve ราคาใหม่คนละ transaction · `oem_metal_price` ของวันนี้ upsert ทับได้ระหว่างวัน ⇒ ต้นทุนที่ล็อกถาวรอาจไม่ใช่เลขที่เจ้าของเห็น | `p_expected_spot_thb_per_gram` — เทียบก่อน stamp ไม่ตรง = raise |
| **M2** ลบ override/หมายเหตุไม่ได้ | `coalesce(new, old)` ⇒ null = ไม่แตะ แต่จอขึ้นว่าบันทึกสำเร็จ | `p_clear_note` / `p_clear_spot_override` |
| **M4** ไม่รู้ว่าใครกด | เรียกผ่าน service client ⇒ `auth.uid()` = null เสมอ | `p_actor` จาก `getSessionUser()` **ฝั่ง server เท่านั้น** |

### 🔴 มติ Tech Lead: ด่านราคาเป็น **fail-closed** (รับข้อเสนอ reviewer, M-a)

โจทย์รอบแรกผมสั่งว่า "caller เก่าต้องไม่พัง" ⇒ `p_expected` เป็น null แปลว่า "ไม่เทียบ"
reviewer แย้งว่า **ไม่มี caller เก่าอยู่จริงเลยในรีโป** (grep แล้ว call site เดียวคือ `ProductionDoneDialog`)
⇒ ข้อกำหนดนั้นกำลังปกป้องสิ่งที่ไม่มีอยู่ แลกกับด่านที่บังคับได้แค่ที่ UI

**ตัดสินใจ: ใบที่ใช้ราคาเงินจริง (`v_needs_spot`) ต้องส่ง `p_expected_spot_thb_per_gram` มาเสมอ — null = raise**
ใบที่ทุกบรรทัดเป็น `fixed` ไม่แตะเงื่อนไขนี้เลย (dry-run ยืนยัน)
⚠️ **ผลที่ตามมา**: ถ้าวันหน้ามี script/MCP/หน้าใหม่ที่เรียก `production_order_done` ต้องส่ง arg นี้ด้วยเสมอ ไม่ใช่ทางเลือก

### 🔴 มติ: ปิดช่อง M-b ด้วย (race ของ `cost_type`)

ถ้ามีใครพลิก `product.cost_type` จาก fixed เป็น spot คั่นกลางระหว่าง statement เช็ค `v_needs_spot`
กับ statement อ่าน `v_product` (READ COMMITTED) ⇒ `v_spot` เป็น null แล้ว `production_cost_calc`
จะ **ไปหยิบราคาสดเอง** ⇒ ข้ามการเทียบ M1 ทั้งดุ้น และไม่สนราคาเฉพาะใบที่เจ้าของกรอก
⇒ เพิ่ม guard ใน `done`: `cost_type='spot'` แต่ `v_spot is null` = **raise ไม่ใช่เดาต่อ**

### หนี้ที่ยังเหลือหลัง 0132 (จดไว้ อย่าให้หาย)

1. **M4 ปิดแค่ 2 จุด** — `production_order_cancel` ยังไม่รู้ว่าใครยกเลิก (ตารางไม่มีคอลัมน์ `cancelled_by` ด้วยซ้ำ) · `item_set`/`item_remove` ไม่มี actor
2. **M2 เปิดช่อง "แท็บเก่าลบค่าคนอื่น"** — ฟอร์มส่ง `clear=true` ทุกครั้งที่ช่องว่าง ⇒ แท็บที่เปิดค้างไว้ (ยังไม่เห็นหมายเหตุที่เพิ่งพิมพ์จากอีกแท็บ) กดบันทึกเพื่อแก้แค่ราคา จะลบหมายเหตุทิ้งไปด้วย · ร้านคนเดียว = เสี่ยงต่ำ แต่เป็นผลข้างเคียงจริงของ M2
3. **`getActorId()` เรียก `getSessionUser()` ซ้ำ** ไม่ผ่าน `React.cache` เหมือน `getEffectiveRole` — +1 round-trip ต่อการเขียน
4. **`/stock/hero` เป็นหน้าสาธารณะที่อ่าน `central_stock` ตัวเดียวกัน** — P1 บวกอย่างเดียว ⇒ ตัวเลขบนหน้านั้นจะสูงกว่าจริงเรื่อยๆ หลังเริ่มใช้ใบผลิต — **ปิดแล้ว 18 ก.ย. 69**: ทั้ง `/stock` และ `/stock/hero` มีแถบเตือนว่ายังไม่หักของที่ขายไปแล้ว และ **P1.5 (0133) ทำให้หักจริงแล้ว**
5. `scripts/verify-0131.sql` ถูกแปะหัวว่า **SUPERSEDED — ห้ามรันซ้ำ** (มี signature เก่าอยู่ข้างใน จะสร้าง overload)

---

## §P1.5 — หักสต็อกจากยอดขาย (0133, apply แล้ว 18 ก.ย. 69)

P1 ทำให้ "ผลิตเสร็จ = ของเข้าสต็อก" แล้ว แต่ **ไม่มีอะไรหักออกตอนขาย** ⇒ ตัวเลขบวกทางเดียว
P1.5 คือตัวที่ทำให้ยอดคงเหลือเป็นจริง ดีไซน์ต้นทาง: `system-flow-2026-09.md` §1

### 🔴 หัวใจ: reconcile ไม่ใช่ "ขายแล้วตัด"

วิธีตรงไปตรงมา (ตัดตอนขาย) **ผิดทั้งสองแบบที่คิดออก**:

| วิธี | พังตอนไหน |
|---|---|
| ผูก idem key กับ `fact_order_item.id` | re-import ลบรายการเดิมทิ้งแล้ว insert ใหม่ ⇒ id เปลี่ยนทุกรอบ ⇒ **ตัดซ้ำทุกครั้งที่นำเข้าไฟล์เดือนเดิม** |
| ผูก key กับ (order, sku) เฉยๆ | ลูกค้าสั่งเพิ่มบนใบเดิมข้ามวันได้ ⇒ จำนวนเปลี่ยนแต่ key เดิม ⇒ `adjust_stock` raise 23505 |

**ทางที่ใช้**: เก็บว่าตัดไปแล้วเท่าไร (`analytics.stock_sale_applied`) เทียบกับยอดที่ควรเป็นจริงตอนนี้
(`target` คำนวณสดจาก `fact_order_item` ทุกครั้ง) แล้วปรับแค่ `delta = target − qty_applied`

ผลที่ได้ฟรีจากโครงนี้ — ไม่ต้องเขียนโค้ดเพิ่มสักบรรทัด:
- นำเข้าไฟล์เดิมซ้ำ ⇒ delta 0 ⇒ ไม่มี ledger row เพิ่มเลย (idempotent โดยธรรมชาติ)
- สั่งเพิ่ม 2→3 ⇒ ตัดเพิ่ม 1
- **ใบถูกยกเลิก ⇒ ของกลับเข้าสต็อกเอง** เพราะ 0115 ลบแถวจริง (ไม่ใช่ปักธง) ⇒ target หาย ⇒ delta ติดลบ
  ⇒ **ไม่ต้องแก้ 0114/0115 เลยแม้แต่บรรทัดเดียว**

### 3 จุดที่เบี่ยงจากถ้อยคำ design — มติ Tech Lead 18 ก.ย. 69

**[A] `track_stock_since` ใช้ `coalesce(เดิม, วันนี้)` ไม่ใช่ set ทับเป็นวันนี้เสมอ**
backend-dev เถียงกลับมาว่าถ้อยคำใน design ขัดกับคอมเมนต์คอลัมน์ที่ 0131 วางไว้บน prod แล้ว — **เขาถูก**
ถ้า set ทับ: ปิดแล้วเปิดใหม่ ⇒ ยอดที่ reconcile ไปแล้วหลุด scope ⇒ ระบบเห็นเป็น "ใบหาย"
⇒ **คืนสต็อกให้ของที่ขายไปแล้วจริง** = บั๊กเงียบที่แก้คืนยากมาก
เพิ่มเอง: **เปิดซ้ำพร้อมยอดตั้งต้น > 0 ถูกปฏิเสธด้วย 22023 ที่บอกทางออก** (เดิมจะไปชน idem key
`init:<product>:<since>` แล้วได้ 23505 ที่อ่านไม่รู้เรื่อง — หรือแย่กว่านั้นคือเงียบไม่บวกให้
ถ้าจำนวนเท่าเดิม ทำให้เข้าใจผิดว่าตั้งยอดใหม่สำเร็จ)

**[B] ฝั่ง applied ของ full outer join ต้องกรอง `p.track_stock` ด้วย**
ถ้ากรองแค่ shop_id/order_no ตามถ้อยคำ design: **กดปิดนับสต็อก = คืนสต็อกที่ตัดไปแล้วคืนทั้งหมดทันที**
ปิดต้อง **freeze** ไม่ใช่ล้าง

**[C] `p_initial_qty = 0` ⇒ ข้าม `adjust_stock`** เพราะ 0007 ปฏิเสธ delta=0
ไม่งั้น SKU ใหม่ที่ยังไม่มีของในมือเลยจะเปิดนับไม่ได้ — ซึ่งเป็นเคสที่พบบ่อยที่สุด

### หลักฐาน (ไม่ใช่รีวิวด้วยตาเปล่า)

- **dry-run ก่อนแตะ DB จริง: ผ่าน 31/31** (`begin;` + DDL + do-block + `raise` บังคับ rollback)
- **post-apply กับของที่ลงไปแล้วจริง: ผ่าน 24/24**
- ยืนยันกับ DB จริงก่อนเขียนโค้ด: `import_delete_orders` ลบจริง · advisory key ตรงกับ 0114/0115 เป๊ะ ·
  `source_order_no` NOT NULL + unique ต่อร้าน · `adjust_stock` กันติดลบและต่ำกว่า reserved
- `get_advisors` ไม่มี WARN ใหม่ (`stock_sale_applied` ขึ้น INFO `rls_enabled_no_policy` ตามที่ตั้งใจ —
  service_role เท่านั้น เหมือน `label_file`/`stg_label_page`)

### หนี้ที่รู้ตัว — ต้องปิดก่อนต่อ wiring/UI

1. **เปิดนับวันไหน ออเดอร์วันเดียวกันที่ขายไปก่อนนับจะโดนหักซ้ำ** (`order_date` เป็น date ไม่มีเวลา —
   ข้อมูลไม่มีเวลาให้ใช้ แก้ที่ logic ไม่ได้) ⇒ **UI ต้องเขียนว่า "ให้นับก่อนเริ่มขายของวันนั้น"**
2. **ปิดนับไว้นานแล้วเปิดใหม่** ⇒ ยอดขายช่วงที่ปิดถูกตัดรวดเดียว ถ้าของไม่พอตกที่ `last_error`
   (fail closed เห็นได้ ไม่เงียบ) — ต้องมี flow "นับใหม่/ปรับยอด" จริง (P2/P3)
3. **ผู้เรียกที่ส่ง `p_source_order_nos` แบบจำกัด ต้องใส่เลขใบที่ถูกลบเข้ามาด้วย** ไม่งั้นของไม่ถูกคืน
   ⇒ `deleteMissingOrders` ต้องส่งเลขใบที่ลบมาด้วย หรือส่ง `null` (กวาดทั้งร้าน)
4. `reorder_point` เพิ่มคอลัมน์ไว้แล้วแต่**ยังไม่มีอะไรอ่าน** — รอ P3a (`v_stock_alert`)
5. ยังไม่มี guard "เปิดนับบนแถวแม่ที่มีแถวลูก" เพราะ `parent_product_id` (P2) ยังไม่มีในสคีมา — รอ P2
